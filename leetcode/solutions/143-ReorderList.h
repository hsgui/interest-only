#include <algorithm>
#include <vector>
#include <memory>
#include <unordered_map>
#include <cassert>

struct ListNode
{
	int val;
	ListNode* next;
	ListNode(int v) :val(v), next(nullptr){}
};

class ReorderList{
public:
	void reorderList(ListNode * head)
	{

	}

	bool Test()
	{

	}
};