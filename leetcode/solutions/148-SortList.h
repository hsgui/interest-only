#include <algorithm>
#include <vector>
#include <memory>
#include <unordered_map>
#include <cassert>

using namespace std;

#include "ListNode.h"

// https://leetcode.com/problems/sort-list/
class SortList
{
public:
	// O(n * log(n))
	ListNode* sortList(ListNode* head)
	{
		return NULL;
	}

	bool Test()
	{
		return true;
	}
};