#include <algorithm>
#include <vector>
#include <memory>
#include <unordered_map>
#include <unordered_set>
#include <cassert>

#include "TreeNode.h"
#include "Utils.h"

using namespace std;

class BinaryTreeZigzagLevelOrderTraversal
{
public:
	// todo: solve this problem using the recursive method
	vector<vector<int>> zigzagLevelOrder(TreeNode* root) 
	{
		vector<vector<int>> levels;

		return levels;
	}
};